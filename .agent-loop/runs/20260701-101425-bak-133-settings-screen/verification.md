# Verification — BAK-133
- swift build → Build complete ✅ (all MustardScreen switches handle .settings) · swift test → 419 pass/1 skip/0 fail ✅
- Acceptance: sidebar ⚙ opens a Settings screen with Sources + Trust; dock hidden on Settings; trust stays in sync with the console (same AppStorage).

# Risk Report — BAK-118
**MEDIUM** → auto-merge after fresh-context review.
- Label: Improvement. Paths: TaskDetailSheet.swift (view), docs. → Sources/ = medium.
- No high paths. No schema change. Subtask toggle uses markDone()/stage; remove uses context.delete (nullify children). No outward actions.

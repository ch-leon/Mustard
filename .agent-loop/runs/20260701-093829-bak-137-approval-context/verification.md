# Verification — BAK-137
- swift build → Build complete ✅ · swift test → 419 pass/1 skip/0 fail ✅
- Acceptance: stage badge + gated notice + confidence + WHY + draft shown when the task has agent context; data from task.confidence / task.delegation. View-only.

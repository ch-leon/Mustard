# Verification — BAK-110
- iOS: `./build-ios.sh` → BUILD SUCCEEDED ✅
- macOS: swift build clean · swift test 419 pass/1 skip ✅ (SPM untouched — MustardMobile not in Package.swift)
- Acceptance: 4 tabs navigate; Agent badge derives from pending recs; FAB shows the placeholder toast on Today/Board; filter state shared Board↔Week.

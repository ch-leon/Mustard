## BAK-114 — iOS Board (stacked sections)

Mobile Board as vertical stacked sections (one per non-empty stage, owner order) with owner + area filter chips (shared with Week), a "● N waiting" pill, and a mobile board card (priority flag, ✦ Agent label, ✦ Proposed, 🔒, area meta, left agent accent, inline gate buttons ✓ Approve & run/✓ Accept + Deny/Discard). Tap opens the shared task-detail sheet. No drag/owner-toggle (mobile). Reuses tested PersonalBoard.

iOS build-ios.sh → BUILD SUCCEEDED · macOS untouched. Risk: medium.

# Risk Report — BAK-132
**MEDIUM** → auto-merge after fresh-context review.
- Label: Improvement. Path: AgentConsoleView.swift (view). NOT TrustPolicy.swift (no high path/label match).
- Selection dispatch unchanged (trustRaw + agent.applyTrust); control UI swap only. No gating logic, schema, or outward action.
